#include <stdio.h>
#include <inode.h>
#include <superblock.h>

int main(int, char**){
    printf("Hello, from divad!\n");

    // inode_t inode = create_inode(1);
    // superblock_t block;
    // printf("%d\n", inode.id);
}
